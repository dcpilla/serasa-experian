
---
#hostname
app_name: @@APP_NAME@@
instance_encrypt: @@INSTANCE_ENCRYPT@@
hostname: "{{ country }}A{{ cloud_region[aws_region] }}{{ acc_env[env] }}APU{{ bu_name[bu] }}{{(groups.@@APP_NAME@@.index(inventory_hostname) + 1)}}"
