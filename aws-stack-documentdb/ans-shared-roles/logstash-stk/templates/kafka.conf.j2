input {
  kafka {
    bootstrap_servers => "{{ kafka_servers }}"
    topics_pattern => [".*"]
    codec => json
    group_id => "logstash"
    auto_offset_reset => "earliest"
    decorate_events => "true"

    }
}